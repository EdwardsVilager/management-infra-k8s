apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wsrecaudos.fullname" . }}-test-connection"
  labels:
    {{- include "wsrecaudos.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wsrecaudos.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
